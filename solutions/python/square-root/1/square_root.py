def square_root(number: int) -> int:
    """
    Determine the square root of a number.
 
    :param number: int - number whose square root we want.
    :return int - square root of the number.
    """
    
    return number ** (1/2)
